# Configure these only in a local .env file or the deployment provider's
# encrypted environment settings. Never expose them through PUBLIC_ variables.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
