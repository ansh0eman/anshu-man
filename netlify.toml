[build]
command = "npm run build"
publish = "dist"

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"

[[redirects]]
from = "/api/spotify/now-playing"
to = "/.netlify/functions/spotify-now-playing"
status = 200
force = true
